# macOS
.DS_Store

# Dependencies
node_modules/

# Expo
.expo/
.expo-shared/
dist/
web-build/

# Native builds
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Env
.env
.env.local
.env.*.local

# Logs
npm-debug.*
yarn-error.*
*.log

# QA audit — RAW SELLER DATA. Real Amazon reports contain order ids, SKUs and
# (in MTR/All-Orders) buyer address fields. Never commit them; only sanitized
# fixtures under qa/fixtures/ are safe to track.
qa-data/

# Python bytecode + audit run output
__pycache__/
qa/out/

# UI audit screenshots contain real order ids and SKU names — never commit
qa-data/screenshots/
